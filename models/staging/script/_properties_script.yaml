version: 2

sources:
  - name: script
    database: pure-silicon-474707-k0
    schema: script
    tables:
      - name: current_player_name
        description: The current username of all event players
        config:
          freshness:
            warn_after: {count: 26, period: hour}
            error_after: {count: 28, period: hour}
          loaded_at_field: updated_at