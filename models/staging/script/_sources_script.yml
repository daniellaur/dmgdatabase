version: 2

models:
    - name: stg_script__mc_usernames
      description: The current mc username of all players
      columns:
      - name: player_id
        tests:
          - not_null
          - unique