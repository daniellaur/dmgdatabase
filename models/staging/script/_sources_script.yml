version: 2

models:
    - name: stg_script__current_player_name
      description: The current username of all event players
      columns:
      - name: player_id
        tests:
          - not_null
          - unique